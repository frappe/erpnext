# ZirakERP Docker Compose Configuration
# Builds custom image from local rebranded source
services:
  backend:
    # ZirakERP application backend
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "backend"
    networks:
      - zirak_network
    restart: on-failure
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-admin}
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD:-admin}

  configurator:
    # ZirakERP site configurator (one-time setup)
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "configurator"
    networks:
      - zirak_network
    restart: "no"
    entrypoint:
      - bash
      - -c
    command:
      - >
        ls -1 apps > sites/apps.txt;
        bench set-config -g db_host $$DB_HOST;
        bench set-config -gp db_port $$DB_PORT;
        bench set-config -g redis_cache "redis://$$REDIS_CACHE";
        bench set-config -g redis_queue "redis://$$REDIS_QUEUE";
        bench set-config -g redis_socketio "redis://$$REDIS_QUEUE";
        bench set-config -gp socketio_port $$SOCKETIO_PORT;
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      REDIS_CACHE: redis-cache:6379
      REDIS_QUEUE: redis-queue:6379
      SOCKETIO_PORT: "9000"
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    depends_on:
      db:
        condition: service_healthy
      redis-cache:
        condition: service_started
      redis-queue:
        condition: service_started

  create-site:
    # ZirakERP site creator (one-time setup)
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "create-site"
    networks:
      - zirak_network
    restart: "no"
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    entrypoint:
      - bash
      - -c
    command:
      - >
        wait-for-it -t 120 db:3306;
        wait-for-it -t 120 redis-cache:6379;
        wait-for-it -t 120 redis-queue:6379;
        export start=`date +%s`;
        until [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".db_host // empty"` ]] && \
          [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".redis_cache // empty"` ]] && \
          [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".redis_queue // empty"` ]];
        do
          echo "Waiting for configurator to set up common_site_config.json...";
          sleep 5;
          if (( `date +%s`-start > 120 )); then
            echo "Timeout: could not find common_site_config.json with required keys";
            exit 1;
          fi
        done;
        echo "Configuration ready!";
        bench new-site --mariadb-user-host-login-scope='%' --admin-password=admin --db-root-username=root --db-root-password=${DB_PASSWORD:-admin} --install-app erpnext --set-default frontend;
    depends_on:
      configurator:
        condition: service_completed_successfully

  db:
    # ZirakERP database
    image: mariadb:10.6
    labels:
      com.zirakerp.service: "database"
    networks:
      - zirak_network
    healthcheck:
      test: mysqladmin ping -h localhost --password=${DB_PASSWORD:-admin}
      interval: 5s
      retries: 20
      start_period: 10s
    restart: on-failure
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-character-set-client-handshake
      - --skip-innodb-read-only-compressed
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-admin}
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD:-admin}
    volumes:
      - db-data:/var/lib/mysql

  frontend:
    # ZirakERP web frontend (Nginx)
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "frontend"
    networks:
      - zirak_network
    depends_on:
      - backend
      - websocket
    restart: on-failure
    command:
      - nginx-entrypoint.sh
    environment:
      BACKEND: backend:8000
      FRAPPE_SITE_NAME_HEADER: frontend
      SOCKETIO: websocket:9000
      UPSTREAM_REAL_IP_ADDRESS: 127.0.0.1
      UPSTREAM_REAL_IP_HEADER: X-Forwarded-For
      UPSTREAM_REAL_IP_RECURSIVE: "off"
      PROXY_READ_TIMEOUT: 120
      CLIENT_MAX_BODY_SIZE: 50m
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    ports:
      - "${HTTP_PORT:-8080}:8080"

  queue-long:
    # ZirakERP long-running background worker
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "queue-long"
    networks:
      - zirak_network
    restart: on-failure
    command:
      - bench
      - worker
      - --queue
      - long,default,short
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379

  queue-short:
    # ZirakERP short-running background worker
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "queue-short"
    networks:
      - zirak_network
    restart: on-failure
    command:
      - bench
      - worker
      - --queue
      - short,default
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379

  redis-queue:
    # ZirakERP job queue (Redis)
    image: redis:6.2-alpine
    labels:
      com.zirakerp.service: "redis-queue"
    networks:
      - zirak_network
    restart: on-failure
    volumes:
      - redis-queue-data:/data

  redis-cache:
    # ZirakERP cache layer (Redis)
    image: redis:6.2-alpine
    labels:
      com.zirakerp.service: "redis-cache"
    networks:
      - zirak_network
    restart: on-failure

  scheduler:
    # ZirakERP task scheduler
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "scheduler"
    networks:
      - zirak_network
    restart: on-failure
    command:
      - bench
      - schedule
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs

  websocket:
    # ZirakERP real-time websocket service
    image: zirakerp:latest
    labels:
      com.zirakerp.service: "websocket"
    networks:
      - zirak_network
    restart: on-failure
    command:
      - node
      - /home/frappe/frappe-bench/apps/frappe/socketio.js
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs

volumes:
  db-data:
  redis-queue-data:
  sites:
  logs:

networks:
  zirak_network:
    driver: bridge
